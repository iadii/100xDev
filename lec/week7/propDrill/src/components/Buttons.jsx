export function Button({count, SetCount}){
    return(
        <div>
            <button onClick={() => {
                
            }}>Increasing</button>
            <button onClick={() => {

            }}>Decreasing</button>
        </div>
    )
}